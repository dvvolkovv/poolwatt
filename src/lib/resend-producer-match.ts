// Stub — full implementation lands in R4 Task 3.
// This file must exist so tests can vi.mock() it.

export async function sendProducerInterestExpressedToOwner(_input: {
  claimId: string;
  buildRequestId: string;
  producerName: string;
  ownerUserId: string;
}): Promise<void> {
  // no-op until Task 3
}
